{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Calendars",
  "type": "object",
  "properties": {
    "calendar_id": {
      "type": "integer",
      "description": "The unique identifier for the calendar."
    },
    "name": {
      "type": "string",
      "description": "The name of the calendar."
    },
    "description": {
      "type": "string",
      "description": "A brief description of the calendar."
    },
    "user_grant": {
      "type": "string",
      "format": "uuid",
      "description": "The UUID grant associated with the user."
    },
    "user_id": {
      "type": "integer",
      "description": "The ID of the user associated with the calendar."
    }
  },
  "required": ["calendar_id", "name", "user_grant", "user_id"],
  "additionalProperties": false
}
