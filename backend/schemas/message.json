{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Messages",
  "type": "object",
  "properties": {
    "message_id": {
      "type": "integer",
      "description": "The unique identifier for the message."
    },
    "chat_group_id": {
      "type": "integer",
      "description": "The identifier of the chat group where the message was sent."
    },
    "sender_id": {
      "type": "integer",
      "description": "The identifier of the user who sent the message."
    },
    "message_text": {
      "type": "string",
      "description": "The text content of the message."
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "The timestamp when the message was sent."
    }
  },
  "required": ["message_id", "chat_group_id", "sender_id", "message_text", "timestamp"],
  "additionalProperties": false
}
